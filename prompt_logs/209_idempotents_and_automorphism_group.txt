Prompt 209 -- Idempotents in §5; standalone automorphism-group calculation
Date: 2026-07-10

User (verbatim intent):
1. Remark 5.2 (exhaustive facts) is redundant with 5.1: the product-norm
   values in Remark 5.1 ALREADY show the order has no idempotents (would
   need norm 8) and no nilpotents (norm 0) on the lattice. Scratch
   Remark 5.2 entirely; append that observation, very concisely, to
   Remark 5.1.
   [User correction mid-turn: the original wording had 5.1 and 5.2
    crossed over. Surviving remark = 5.1 (product norms); scratched =
    5.2 (exhaustive facts). The exhaustive-search script stays cited as
    the ground truth for the self-product values.]
2. MORE INTERESTING: the ALGEBRA does permit idempotents, even though
   the lattice does not. On the lattice this corresponds to rescaling.
   Three are immediate: the octonion unit in one of the three blocks,
   zeros in the other two. §5 must describe the idempotents we can
   find. The script-referencing style correctly identifies the
   author's contribution: an engineer who employs machine tooling
   (GAP/LOOPS, Python) to FIND properties, not an abstract
   mathematician who explains why they are meaningful.
3. Calculate the automorphism group of the product in this repo.
   Deliver a SELF-CONTAINED TeX/PDF for the calculation. The user will
   review it and decide how to work it into the paper.

Also noted by user: the AI review quality has improved markedly; this
raises the bar on delineating clearly what the author can honestly
defend versus what lies outside his expertise. All new material must
be scoped accordingly (report what is computed and verified; do not
interpret).

Plan (ultracode; Workflow with adversarial verification):
Phase 1 (compute, parallel):
  A. Ideal decomposition + idempotent classification. Hypotheses to
     VERIFY OR REFUTE independently (derived by the orchestrator, not
     to be trusted):
       - R^24 = D (+) T with D = diagonal {(a,a,a)} (8-dim) and
         T = sum-zero {(p,q,r) : p+q+r = 0} (16-dim);
         D*D <= D, T*T <= T, D*T = T*D = 0 (two-sided ideals,
         zero cross-products) -- one-line consequence of bilinearity.
       - Idempotents include eps_1 = (e_0,0,0) and its two block
         images; omega/3 = (1/3)(e_0,e_0,e_0); (2/3,-1/3,-1/3)e_0 and
         permutations.
       - Lattice connection: e_0 not in L, so no eps_i lies in Lambda;
         but for u = (2 lambda, 0, 0) with lambda a PURE IMAGINARY
         root, u*u = (-8 e_0, 0, 0) = -8 eps_1 -- i.e. the idempotent
         is a rational rescaling of a lattice square.
     Deliverable: exact classification (or an honest statement of what
     is and is not classified), scripts, report.
  B. Automorphism group of the product. Hypotheses to verify/refute:
       - Aut preserves D and T (different dimensions).
       - (D, *) = (O, 3 x .sigma), so Aut(D) = Aut(O) = G_2.
       - C_3-Fourier diagonalisation: with u_hat_j = sum_alpha
         zeta^{j alpha} u_alpha, the product satisfies
         P_hat_k = u_hat_{2k} .sigma v_hat_{2k}; hence over C,
         T (x) C = O_C (+) O_C with the SWAP-twisted product
         (a_1,a_2)(b_1,b_2) = (a_2 b_2, a_1 b_1).
       - Aut(R^24, *) = Aut(D) x Aut(T) (independent ideals).
       - Aut(Lambda, +, *) = {g in Aut(R^24,*) : g(Lambda) = Lambda},
         FINITE. Compute its order and structure exactly.
       - OPEN in the paper and to be SETTLED here: is
         Aut(Lambda,+,*) contained in Co_0 (does it preserve the Leech
         quadratic form)? The v6 Conclusion asserts this via an
         "equivalently the stabiliser inside Co_0" gloss that the
         journal review flagged as unproven (finding R3).
     Deliverable: exact group order + structure (GAP
     StructureDescription), scripts, report.
Phase 2: adversarial verification (independent re-derivations).
Phase 3: self-contained TeX for the automorphism-group calculation.
Then (orchestrator): the §5 paper edits, using only verified facts.

Status: CLOSED (see the results and closure recorded below).

--------------------------------------------------------------------------
TASK B RESULT (automorphism group of the product) -- computed 2026-07-12
--------------------------------------------------------------------------
  Aut(Lambda, +, star)  has ORDER 36 and structure  C_6 x S_3
  (GAP StructureDescription; centre C_6, derived subgroup C_3, -I not in it).

  Every element is (blockwise A) o (block permutation) with A in a C_6 of
  octonion automorphisms; the C_6 is generated by the signed permutation
    e_0 -> e_0, e_1 -> -e_5, e_2 -> -e_7, e_3 -> e_2, e_4 -> -e_4,
    e_5 -> e_6,  e_6 -> e_1,  e_7 -> -e_3.

  Ambient algebraic group:  Aut(R^24, star) = (G_2 x G_2) x S_3, dim 28,
  COMPACT (both G_2 are the compact real form).  D and T are recovered as
  Ann(T) and Ann(D), so every star-automorphism preserves them.

  Co_0 QUESTION -- SETTLED, ANSWER YES.  Aut(Lambda,+,star) <= Co_0, and the
  reason is compactness, not the trace form: an algebra automorphism of the
  REAL octonions preserves the positive-definite norm, D and T are orthogonal
  complements, so every star-automorphism of R^24 is in O(24).  The
  star-intrinsic trace form tr(L_u L_v) = 24 * sum_alpha Re(u_alpha .s v_alpha)
  is diagonal of signature (3,21) -- NOT the Leech form, and not even
  alpha<u_D,v_D> + beta<u_T,v_T>; that route fails.

  REFUTED en route: the "12096 = |G_2(2)|" figure is the stabiliser of
  Ls_bar, not of L.  Stab(L) = 1344 (= 2^3.L_3(2)), Stab(Ls) = 168
  (= 2^3:(7:3)), Stab(Ls_bar) = 12096 (= U_3(3).2 = G_2(2)).

  Scripts: python_project/src/verify_star_algebra_structure.py,
           python_project/src/verify_aut_lambda_star.py,
           python_project/src/verify_aut_octonion_crosscheck.py,
           gap_project/aut_lambda_star.g, gap_project/octonion_stabilisers.g

--------------------------------------------------------------------------
TASK A RESULT (idempotents) -- computed 2026-07-12
--------------------------------------------------------------------------
  (R^24, +, star) has EXACTLY EIGHT idempotents.  Complete classification,
  not a count over a sample or a shell.  All eight are real and lie in the
  3-dimensional subalgebra A_3 = span{eps_1, eps_2, eps_3}:

      0;   eps_1 = (e_0,0,0), eps_2 = (0,e_0,0), eps_3 = (0,0,e_0);
      w/3 = (1/3)(e_0,e_0,e_0);
      eps_i - w/3 = (1/3)(2e_0, -e_0, -e_0)  and its two block-cyclings.

  The variety is FINITE (8 reduced points): no positive-dimensional families,
  no idempotent with a nonzero imaginary part.  The user's three "immediate"
  idempotents (octonion unit in one block, zeros elsewhere) are eps_1, eps_2,
  eps_3, and the classification shows they generate the rest.

  Engine: R^24 = D (+) T, mutually annihilating two-sided ideals, so u = d + t
  is idempotent iff d and t are; (D, star) = (O, .s) via phi(a) = (1/3)(a,a,a);
  O is a division algebra, so the only D-idempotents are 0 and w/3; the
  imaginary part is then killed by an exact eigenvalue argument plus an exact
  solve over C.

  The algebra has NO identity element.  w = (e_0,e_0,e_0) is NOT idempotent
  (w star w = 3w).

  LATTICE CONNECTION (the user's "rescaling"): no idempotent lies in Lambda
  (e_0 is not in L), but each spans a rational ray that MEETS Lambda.  Least
  positive multiples in Lambda:  4 eps_i (norm 16); 6(eps_i - w/3) (norm 24);
  12(w/3) (norm 48).  In each case u = nE satisfies u star u = n u.
  Witness confirmed exhaustively for all 84 purely imaginary E_8 roots:
  u = (2 lambda, 0, 0) is in Min(Lambda) and u star u = (-8 e_0, 0, 0) =
  -8 eps_1, which is in Lambda.  CORRECTION to the plan's framing above:
  the minimal lattice point on the eps_1 ray is -4 eps_1, not -8 eps_1.

  Scripts: python_project/src/verify_ideal_decomposition.py,
           verify_idempotent_classification.py,
           verify_square_zero_classification.py,
           verify_idempotent_lattice_rescaling.py

--------------------------------------------------------------------------
ADVERSARIAL VERIFICATION -- 2026-07-12
--------------------------------------------------------------------------
Two independent verifiers rebuilt both results from the paper's definitions
without running or importing any first-pass script.  All load-bearing claims
CONFIRMED (order 36, structure C_6 x S_3, generators, ambient group, the Co_0
containment, the eight idempotents).  Four disagreements, recorded rather than
papered over:

 1. PROOF GAP (conclusion correct, stated reason CIRCULAR).  The Task B block
    above says "D and T are recovered as Ann(T) and Ann(D), so every
    star-automorphism preserves them."  That is circular: g(Ann(D)) = Ann(g(D))
    yields g(T) = T only once g(D) = D is already known, and the annihilator
    relation alone does not make D intrinsic.  CORRECTED ROUTE (now printed in
    the note and in the docstring of verify_star_algebra_structure.py): D and T
    are SIMPLE ideals of different dimension (their multiplication algebras are
    the full matrix algebras, dim 64 and 256, computed exactly), and D star T =
    T star D = 0 makes the projections algebra homomorphisms; hence pi_T(g(D))
    is an ideal of T of dimension <= 8 < 16, so it is 0, so g(D) = D, and only
    THEN does g(T) = T follow.
 2. MISLABELLED DATA.  The first pass reported element orders
    "1,2,2,2,3,3,3,3,3,6(x9)".  Those are the orders of the 18 conjugacy-class
    REPRESENTATIVES, not of the 36 elements.  True distribution: 1(x1), 2(x7),
    3(x8), 6(x20).  gap_project/aut_lambda_star.g now prints both, labelled.
 3. Artin's theorem (any two octonions generate only a quaternion subalgebra)
    is WHY the stabiliser enumerations must use generating TRIPLES; a reader
    checking with pairs gets a wrong, much larger count.  Stated in the note.
 4. Lambda as realized by Wilson's conditions is sqrt(2) x Leech (minimal norm
    8, Gram determinant 2^24), so no unimodularity claim should attach to it.
    Does not affect Aut (Co_0 is scale-invariant).

Orchestrator's own independent confirmation (not the workflow's): GAP run
directly on gap_project/aut_lambda_star.g reports Size = 36,
StructureDescription = C6 x S3, centre C6, derived subgroup C3, -I_24 not in G,
element orders 1(x1) 2(x7) 3(x8) 6(x20).

--------------------------------------------------------------------------
DELIVERABLE
--------------------------------------------------------------------------
paper/automorphism_group_2026-07-12.tex / .pdf -- 11 pages, dated 12 July 2026,
self-contained, builds clean (zero overfull boxes, no undefined references).
Awaiting the user's review; he will decide how it is worked into the paper.

Status: CLOSED.  Computation, write-up, and all resulting paper edits are
done.  Item 2 (the idempotents in Section 5) landed as Remarks 5.3 and 5.4 of
the new Section 5.2 (prompt log 217).  Review findings R3 and recommended 8,
9, 10 landed as Remark 5.5 (Section 5.3) and the rewritten Conclusion bullets
(prompt log 216).  The square-zero half of the classification also corrected a
false statement in the paper; see prompt log 217.
